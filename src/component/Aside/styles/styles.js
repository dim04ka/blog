import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    border: '1px solid red',
    background: 'blue',
    position: 'fixed',
    width: '25%',
    left: 0,
    top: 0,
    right: 0,
    bottom: 0
  }
});
