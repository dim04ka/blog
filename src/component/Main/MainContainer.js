import React from 'react';

const MainContainer = () => {
  const MainContainerText = 'MainContainer';
  return (
    <div>
      { MainContainerText }
    </div>
  );
};

export default MainContainer;
